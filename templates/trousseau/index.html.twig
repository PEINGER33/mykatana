{% extends 'base.html.twig' %}

{% block title %}Liste des trousseaux de tous les membres :{% endblock %}

{% block body %}
<h1>Trousseaux index</h1>
<ul>
  {% for trousseau in trousseaux %}
  <li><a href="{{ path('trousseau_show', {'id': trousseau.id}) }}"> Trousseau n° {{ trousseau.id }}</a></li>
  
  {% endfor %} {# trousseau #}
</ul>
  
{% endblock %}
